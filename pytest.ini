[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --alluredir=allure-results
markers =
    unit: framework unit tests
    smoke: critical build verification tests
    sanity: focused tests for recent changes
    regression: full regression suite
    api: api test cases
    db: database validation tests

